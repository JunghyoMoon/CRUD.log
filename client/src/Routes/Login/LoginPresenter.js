import React from "react";
import styled from "styled-components";

const Container = styled.div`
    padding-top: 100px;
`;

const LoginPresenter = () => <Container>login</Container>;

export default LoginPresenter;
