import React from "react";
import styled from "styled-components";

const Container = styled.div`
	padding-top: 100px;
`;

const CreatePresenter = () => <Container>create article</Container>;

export default CreatePresenter;
